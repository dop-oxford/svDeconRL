# svDeconRL
The repository contains the MATLAB codes requires for deconvolving an image with a spatially variant point response using a modified Richardson-Lucy algorithm with total variation regularisation and sample datasets.
